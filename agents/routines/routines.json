{
  "note": "Exported Claude Code routines (scheduled tasks) for the Fable Computer Agent Lab. Import with install_routines.py; see README.md. Placeholders: {{REPO}} = checkout path, {{PLATFORM_NOTE}} = interpreter/encoding note. Definition only — the app's runtime state (createdAt, lastRunAt, lastScheduledFor, notifySessionId) is intentionally not exported.",
  "exported_from": "Windows",
  "routines": [
    {
      "id": "agent-lab-assess-session",
      "displayName": "3. Agent lab assess session",
      "description": "Fable Computer Agent Lab: assess the latest unassessed Fable session (2-of-3 vote) and open the promotion PR",
      "schedule": "manual",
      "enabled": true,
      "useWorktree": false,
      "cwd": "{{REPO}}"
    },
    {
      "id": "agent-lab-fable-session",
      "displayName": "2. Agent lab fable session",
      "description": "Fable Computer Agent Lab: run a Fable session in-session (3 candidate prompts → 2-of-3 vote → execute winner → publish transcript)",
      "schedule": "manual",
      "enabled": true,
      "useWorktree": false,
      "cwd": "{{REPO}}"
    },
    {
      "id": "agent-lab-full-run",
      "displayName": "5. Agent lab full run",
      "description": "Fable Computer Agent Lab: full pipeline — posts → fable session → assess → rework — looping. Mixed models per seat (2026-08-13): Fable 5 for execution + pre-pub checks, Opus 5 for multi-seat stages. MANUAL ONLY — run from \"Run now\".",
      "schedule": "manual",
      "enabled": true,
      "useWorktree": false,
      "cwd": "{{REPO}}"
    },
    {
      "id": "agent-lab-posts",
      "displayName": "1. Agent lab posts",
      "description": "Fable Computer Agent Lab: write and post the three personas' lab posts in-session. MANUAL ONLY — no schedule, run from \"Run now\". Cadence is roughly daily, as compute allows; several runs a day are fine",
      "schedule": "manual",
      "enabled": true,
      "useWorktree": false,
      "cwd": "{{REPO}}"
    },
    {
      "id": "agent-lab-weekly-note",
      "displayName": "4. Agent lab weekly note",
      "description": "Fable Computer Agent Lab: run the weekly-note pipeline in-session on demand (draft → PR → 2-of-3 review vote)",
      "schedule": "manual",
      "enabled": true,
      "useWorktree": false,
      "cwd": "{{REPO}}"
    }
  ]
}
