Run the Fable Computer Agent Lab ASSESSMENT pipeline in-session: decide whether the latest Fable session's result enters the permanent record. You are the project's executor: a maintainer-operated Claude Code session per agents/README.md ("Operations"). This task is manually triggered by the maintainer (Ryoji, github: ryoji-info).

EXECUTION MODE (maintainer request, 2026-07-17 — Fable 5 + orchestration by default):
- MODELS BY SEAT (maintainer policy, 2026-08-13 — supersedes this routine's 2026-07-17 model default; authoritative text: the full-run routine's MODEL section, applied here to a standalone run): the session model is claude-fable-5 (orchestration and any rework drafting); if the live session is on a different model, proceed and disclose it at the TOP of the report. All assessor and re-assessor seats run claude-opus-5 (model: "opus"), set explicitly on every spawn — the record's practice since #115/PR #116; #117 ran nine Opus seats over three rounds. Fallback is claude-opus-5, recorded verbatim, disclosed per seat; a session-limit-interrupted seat is relaunched fresh after the reset with the loss disclosed in that round's record (precedent: 2026-08-12). agent_fable_assess.py's briefing template hardcodes that the reply was "produced by Claude Fable 5" — brief assessors with the reply's ACTUAL executing model whenever they differ; the script predates this policy, so extend it at run time rather than follow it into a mislabel. Disclosure is per seat in every posted vote record, the PR body and the note (precedent: the executing-model block in notes/2026-08-13-walk-excess-clamp-placement.md); the report states the session model plus a per-seat summary.
- The maintainer has standing-authorized multi-agent orchestration for this routine (his 2026-07-17 "run all routines on Fable 5 ultracode by default" instruction, relayed here — its model default is superseded by the 2026-08-13 policy above; the orchestration opt-in remains in force): use the Workflow tool for the substantive stages. Do not treat the absence of the "ultracode" keyword as a block — this paragraph is the explicit opt-in. Orchestration shape for this pipeline: three INDEPENDENT parallel assessor agents (one persona each, blind to each other's verdicts — this is what makes the vote a real 2-of-3; post all three verbatim); each assessor must re-execute the session's key numbers itself against the released code, not trust the transcript.

Repo: {{REPO}} (github.com/ryoji-info/FableComputer). GitHub token: TOKEN=$(printf "protocol=https\nhost=github.com\n" | git credential fill 2>/dev/null | grep '^password=' | cut -d= -f2). Use the token inline via $(...) substitution only; NEVER write the token to a file (2026-08-13 remediation, disclosed in #117's round-2 record). {{PLATFORM_NOTE}}

The specification is agents/scripts/agent_fable_assess.py — read it first and follow its exact flow, but YOU perform the three personas' assessments (no Anthropic API calls). Hard rules: never push to main — promotion goes through a PR branch (naming per the script / project precedent: agents/promote-<date>-<slug>), a human merges per GOVERNANCE.md; the appended vote record is evidence and is never edited afterward; disclose "maintainer-operated Claude Code session per agents/README.md".

1. TARGET + STATE CHECK: git fetch; find the most recent "Fable Session — …" Discussion (GraphQL, orderBy UPDATED_AT desc) that has NOT yet been assessed (no assessment comments on it, no corresponding promotion PR or notes/ file). If everything is already assessed, stop and report that. If an assessment was started but not finished (e.g. votes posted, PR missing), resume from where it stands.
2. Assessment per the script: each persona (fabric 🧵, kinetic 🌊, quanta ⚛️ — adopt from agents/personas/ with all standing rules) independently assesses the session's published reply for the permanent record: re-execute the reply's key numbers against the released model code in fable-model-quantum/ and fable-model-chain/, check consistency with every promoted note in notes/ (the corrected record), and vote per the script's format. 2-of-3 to store. Where the reply bears on a promoted note that registers a standing falsifier, executing it is in-scope assessor work, recorded in the verdict (precedent: #117's rounds executed the mirrored-run falsifiers at N=480/720/960 — all passed); a fresh falsifier the new note itself registers as never-run stays never-run — it is the community's lever, not the assessor's. A store WITH required edits is not a clean pass: post the three verdicts verbatim to the session Discussion and hand off to rework — the full-run routine's STAGE 4 is its specification (fresh mutually blind seats per round, cap 4 with scope reduction past it, precedents PR #98 / discussion #103, and every claimed defect re-executed against the released code before it is accepted OR declined).
3. On a pass: produce the note file for notes/ per the script and project precedent (metadata header, Status line, method, labels; the deciding round's full per-persona vote record appended verbatim under "## Agent assessment — <date>", earlier rounds permanent on the Discussion), push it to a fresh promotion branch via the GitHub API, open the PR with the script's/precedent's body text and the agents:approved-2of3 label — and in the SAME PR maintain the navigation layer per the 2026-07-26 finding-5 rule: the new note's notes/INDEX.md row, updates to any INDEX rows the note changes, and a dated post-promotion annotation appended to each corrected note (precedent: PRs #116 and #118). Do NOT merge. On a fail: post the vote record to the session discussion per the script and open nothing.
4. Report: vote tally, PR link (or the fail record link), one-line summary of what was stored/rejected, plus the model self-check line. If any step fails, report exactly how far it got.