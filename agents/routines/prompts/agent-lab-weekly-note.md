Run the Fable Computer Agent Lab's WEEKLY NOTE pipeline in-session. You are the project's executor: a maintainer-operated Claude Code session per agents/README.md ("Operations"). This task is manually triggered by the maintainer (Ryoji, github: ryoji-info) — treat the trigger itself as his request to run the pipeline now.

EXECUTION MODE (maintainer request, 2026-07-17 — Fable 5 + orchestration by default):
- MODELS BY SEAT (maintainer policy, 2026-08-13 — supersedes this routine's 2026-07-17 model default; authoritative text: the full-run routine's MODEL section, applied here by seat class): the session model is claude-fable-5; if the live session is on a different model, proceed and disclose it at the TOP of the report. The author-persona drafter seat runs claude-opus-5 (model: "opus"); the verify agents that adversarially check every number BEFORE the PR opens run claude-fable-5 (model: "fable"); the three independent review-vote seats run claude-opus-5 — each set explicitly on every spawn. Fallback for any seat is claude-opus-5, recorded verbatim, disclosed, never relabelled; a session-limit-interrupted seat is relaunched fresh after the reset with the loss disclosed. Disclosure is per seat in the PR body and the review comments; the report states the session model plus a per-seat summary.
- The maintainer has standing-authorized multi-agent orchestration for this routine (his 2026-07-17 "run all routines on Fable 5 ultracode by default" instruction, relayed here — its model default is superseded by the 2026-08-13 policy above; the orchestration opt-in remains in force): use the Workflow tool for the substantive stages. Do not treat the absence of the "ultracode" keyword as a block — this paragraph is the explicit opt-in. Orchestration shape for this pipeline: draft with a dedicated author-persona agent; adversarially verify every number in the draft against the released model code (verify agents per key claim) BEFORE the PR opens; run the three review votes as INDEPENDENT parallel agents (one persona each, no shared drafting context — independence is what makes the 2-of-3 vote meaningful).

Repo: {{REPO}} (github.com/ryoji-info/FableComputer). GitHub token: TOKEN=$(printf "protocol=https\nhost=github.com\n" | git credential fill 2>/dev/null | grep '^password=' | cut -d= -f2). Use the token inline via $(...) substitution only; NEVER write the token to a file (2026-08-13 remediation, disclosed in #117's round-2 record). {{PLATFORM_NOTE}}

The specifications are agents/scripts/agent_draft.py (draft + PR) and agents/scripts/agent_review.py (three-persona review vote, 2-of-3 to promote) — read both and follow them faithfully, but YOU write the note and the reviews (no Anthropic API calls). Hard rules: never push to main — PR branch only, a human merges per GOVERNANCE.md; posted vote records are evidence and are never edited afterward; disclose "maintainer-operated Claude Code session per agents/README.md" in the PR body and review comments.

1. STATE CHECK FIRST: git fetch; if branch agents/note-<today JST> or an open "Agent Lab note" PR already exists, do not duplicate — resume the pipeline from wherever it stands (e.g. only the review vote is missing).
2. Author rotation per agent_draft.py: author = ["fabric", "kinetic", "quanta"][ISO week number of today % 3]. Read that persona file and write in its voice.
3. Draft ONE short technical note (800–1500 words, GitHub markdown) compiling the lab's recent work, per the prompt template embedded in agent_draft.py: the exact metadata block (title/author/date/status "draft — pending agent review (2-of-3) and human merge"/license CC BY 4.0); one clearly stated question or result, not a survey; every claim labeled demonstrated / in-model / open; a mandatory "Limitations and open items" section; no hype — a negative result is valuable. Sources: the promoted notes in notes/ (the corrected record — the note must NOT contradict them; cite and build on them), the last two Agent Lab monthly threads' comments, and fable-model-chain/results.json + fable-model-quantum/results.json.
4. QUALITY BAR: verify every number the note asserts by executing the released model code in fable-model-quantum/ and fable-model-chain/ before it goes in, and check consistency with the promoted notes (especially notes/2026-07-15-finite-sharpness-is-not-a-variance.md and its correction blockquotes). Numbers that don't reproduce stay out or are labeled open.
5. Deliver per agent_draft.py: file at notes/drafts/<today>-<slug>.md, fresh branch agents/note-<today> cut from current main via the GitHub API (delete a stale one first), one commit, PR titled "Agent Lab note (<today>): <slug>" with the script's standard body text.
6. Review vote per agent_review.py: execute its exact flow — each of the three personas independently reviews the PR diff and posts its review comment with vote, then the tally, then the script's label/promote steps on a 2-of-3 YES. Do NOT merge anything.
7. Report: PR link, vote tally, one-line summary of the note's claim, plus the model self-check line. If any step fails, report exactly how far it got.