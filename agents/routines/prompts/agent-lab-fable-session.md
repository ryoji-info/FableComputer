Run a Fable Computer Agent Lab FABLE SESSION in-session. You are the project's executor: a maintainer-operated Claude Code session per agents/README.md ("Operations"). This task is manually triggered by the maintainer (Ryoji, github: ryoji-info) — the trigger is his spend decision for a premium-tier session.

EXECUTION MODE (maintainer request, 2026-07-17 — Fable 5 + orchestration by default):
- This routine is meant to run on Claude Fable 5 — for this pipeline that matters doubly, because YOU are the "Fable 5" the session promises. Check which model you are; if you are not Fable 5, still complete the run, but flag the actual model at the TOP of your report AND disclose the executing model honestly in the published transcript (never label output "Fable 5" if it is not).
- The maintainer has standing-authorized multi-agent orchestration for this routine (his 2026-07-17 "run all routines on Fable 5 ultracode by default" instruction, relayed here): use the Workflow tool for the substantive stages. Do not treat the absence of the "ultracode" keyword as a block — this paragraph is the explicit opt-in. Orchestration shape for this pipeline: three INDEPENDENT candidate-prompt agents (one persona each, blind to each other); independent persona vote agents (no self-votes); execute the winning prompt at full depth yourself, then adversarially verify its key numbers with independent check agents against the released code BEFORE publishing.

Repo: {{REPO}} (github.com/ryoji-info/FableComputer). GitHub token: TOKEN=$(printf "protocol=https\nhost=github.com\n" | git credential fill 2>/dev/null | grep '^password=' | cut -d= -f2). {{PLATFORM_NOTE}}

The specification is agents/scripts/agent_fable_session.py — read it first and follow its exact flow, but YOU perform every role (no Anthropic API calls): the three personas' candidate prompts, the vote, AND the Fable execution.

1. STATE CHECK FIRST: git fetch; list recent Discussions (GraphQL, orderBy UPDATED_AT desc). If a "Fable Session — <today JST>" discussion already exists, stop and report it — the next step for an existing session is the assessment task, not a duplicate session.
2. Candidate prompts: each persona (fabric 🧵, kinetic 🌊, quanta ⚛️ — adopt each from agents/personas/, honoring all standing rules incl. promoted-notes grounding) drafts one candidate prompt for a Fable 5 research session, grounded in the current promoted notes (notes/), recent lab-thread discussion, and the released model code — exactly as the script's prompt template specifies.
3. Vote per the script: each persona votes for the best candidate, NO SELF-VOTES; 2-of-3 (or the script's tie-break) selects the winner.
4. Execute the winning prompt yourself at full depth with the project materials the script attaches (papers/, fable-model-quantum/, fable-model-chain/, notes/). House quality bar: verify every number you assert by executing the released code; label every claim demonstrated / in-model / open; a negative or inconclusive answer is a valid result.
5. Publish per the script: a new Discussion titled "Fable Session — <today JST>: <short title>" in the same category the script uses, containing the three candidates, the vote record, the winning prompt, and the full session reply, with the standard disclosure header ("maintainer-operated Claude Code session per agents/README.md"). Vote records are evidence — never edit them after posting.
6. Report: discussion link, the winning prompt's author and question, a one-line summary of the session's verdict, plus the model self-check line. Remind the maintainer that the assessment (agent-lab-assess-session task) is the follow-up step if the reply looks note-worthy. If any step fails, report exactly how far it got.